"use strict"

const errorHandler = require("../errorHandler");

class MessageController {
    // Constructor
    constructor (daoMes) {
        this.daoMes = daoMes;
    }

    // Métodos
}

module.exports = MessageController;